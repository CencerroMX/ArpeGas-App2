export const environment = {
  production: false,
  // Backend PHP (api_app) desplegado en IONOS.
  apiBaseUrl: 'https://corparpe.com/plataforma/api_app',
  // false = usa el backend real; true = respuestas simuladas (sin backend).
  useMock: false,
  appVersion: '0.0.1',
};
